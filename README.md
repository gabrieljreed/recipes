Create new recipe

```bash
hugo new recipes/name-of-recipe.md
```

Start dev server
(You'll need to install Hugo and Go to run the dev server locally)

```bash
hugo server
```

Pushing to the main branch will trigger a new deploy
